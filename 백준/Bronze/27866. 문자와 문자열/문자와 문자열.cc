/**
 * [BRONZE 5] 문자와 문자열
 * @writer : nnlnuu
 */
#include<bits/stdc++.h>
using namespace std;
#define FASTIO ios::sync_with_stdio(0),cin.tie(0);
int main(void){
    string str;
    int idx;
    cin >> str;
    cin >> idx;
    cout << str[idx-1];

    return 0;
}